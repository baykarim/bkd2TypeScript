// src/index.ts
console.log("🚀 Hello GitHub + TypeScript!");

// Petite fonction utilitaire
function greet(name: string): string {
    return `Bonjour, ${name}! 👋`;
}

const user = "Karim";
console.log(greet(user));
